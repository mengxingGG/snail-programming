const fs = require('fs'); const data = JSON.parse(fs.readFileSync('audit_report.json', 'utf8')); data.forEach(d => { if (!d.actual || (!d.actual.includes('ModuleNotFoundError') && !d.actual.includes('SyntaxError') && !d.actual.includes('ReferenceError') && !d.actual.includes('TypeError'))) { console.log('--- ' + d.lang + ' ' + d.id + ' ---'); console.log('Details: ' + d.details); } });
