# 蜗牛编程 — 环境变量模板
# 复制此文件为 .env，填入实际值（.env 已在 .gitignore 中，不会提交）
#
# 注意：这是一个 Electron 桌面应用。大部分配置（特别是 AI 密钥）
# 在运行时通过"设置页面"写入 userData/ai-config.json，不依赖 .env。
# 以下变量仅用于开发阶段的覆盖和调试。

# ─── Vite 开发服务器 ─────────────────────────────────────

# 覆盖开发服务器端口（默认 5173）
# VITE_PORT=5173

# ─── AI 助教（开发环境快速配置）────────────────────────
#
# 生产用途：通过 App 设置页配置，存储在 userData/ai-config.json
# 开发用途：设置以下变量可跳过 UI 配置（需在代码中读取）
#
# AI 服务地址（OpenAI 兼容）
# 示例：
#   OpenAI:   https://api.openai.com/v1
#   DeepSeek: https://api.deepseek.com/v1
#   Ollama:   http://localhost:11434/v1
VITE_AI_BASE_URL=http://localhost:11434/v1

# AI 模型名称
VITE_AI_MODEL=qwen2.5:7b

# ─── 说明 ────────────────────────────────────────────────
#
# AI_API_KEY 不在此文件中定义。
# 密钥只能通过 App 内设置页填写，存储在本地 userData 目录，
# 确保密钥不出现在任何代码文件或版本控制历史中。
